const Title = ({ text }: { text: string }) => {
  return (
    <h2 className='text-[36px] leading-[54px] font-bold text-center text-caribbeanGreen font-serif'>
      {text}
    </h2>
  );
};

export default Title;
